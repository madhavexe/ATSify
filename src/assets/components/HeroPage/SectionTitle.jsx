import React from 'react'

const SectionTitle = (props) => {
  return (
    <p className='section-title text-[2.5rem] font-semibold text-center leading-14 md:w-[65%]'>{props.title}</p>
  )
}

export default SectionTitle